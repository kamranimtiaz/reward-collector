# ===================================================================
# Render Cron Job Environment Variables Template
# ===================================================================
# Copy this file's contents and set each variable in Render Dashboard
# under Environment > Environment Variables
# ===================================================================

# ===== SOLANA RPC CONFIGURATION =====
# Your Solana RPC endpoint (Helius, QuickNode, or public RPC)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# ===== TOKEN CONFIGURATION =====
# The token mint address you want to monitor for reward distribution
TOKEN_MINT=YourTokenMintAddressHere

# ===== PRIVATE KEYS (JSON Array Format) =====
# IMPORTANT: These must be in JSON array format: [1,2,3,4,...]
# To convert from Solana CLI keypair file to JSON array:
#   cat your-keypair.json
# Then copy the array values

# Pump.fun developer wallet private key (receives creator fees)
PUMPFUN_DEVELOPER_PRIVATE_KEY=[1,2,3,4,5,...]

# Pool owner wallet private key (authorized to distribute rewards)
POOL_OWNER_PRIVATE_KEY=[1,2,3,4,5,...]

# ===== REWARD POOL IDL (REQUIRED) =====
# The complete IDL JSON from your deployed program
# To get this value:
#   1. After deploying your program, find: target/idl/reward_pool.json
#   2. Minify it: cat target/idl/reward_pool.json | jq -c
#   3. Copy the entire minified JSON string below
REWARD_POOL_IDL={"address":"YourProgramIdHere","metadata":{"name":"reward_pool","version":"0.1.0","spec":"0.1.0"},"instructions":[...],"accounts":[...],"errors":[...],"types":[...]}

# ===== OPTIONAL CONFIGURATION =====

# Minimum SOL threshold before collecting rewards (default: 0)
REWARD_THRESHOLD_SOL=0.0001

# Fee buffer to retain in developer wallet for transaction fees in lamports (default: 5000)
CREATOR_TRANSFER_FEE_BUFFER_LAMPORTS=5000

# ===== OPTIONAL: DIRECT ADDRESSES (Skip IDL derivation) =====
# If you want to specify addresses directly instead of deriving from IDL

# Direct vault address (if not set, will be derived from REWARD_POOL_IDL)
# VAULT_ADDRESS=YourVaultAddressHere
